---
section: 4
plan: 1
wave: 2
word_budget: 750
---

# Plan 04-01: System Implementation + HDF5/NetCDF Connectors + Experimental Platform

## Context
This plan covers all implementation details in a single plan: the core system stack, DuckDB schema, HDF5 and NetCDF connector specifics, and the experimental platform description. This section provides the reproducibility details that an SC audience expects.

## Tasks

<task type="auto" mode="co-author">
  <name>Core system stack</name>
  <action>Write ~150 words covering the technology stack. Language: Python 3.11+. API: FastAPI with async endpoints (/query, /documents, /jobs/index, /health, /metrics). Storage: DuckDB (embedded, single-file, ACID). Embeddings: sentence-transformers with all-MiniLM-L6-v2 (22.7M parameters, 384-dim). ANN: ExactANNAdapter (sharded, 16 shards) for exact cosine similarity or HnswANNAdapter (hnswlib) for approximate. CLI: Click-based with subcommands (clio query, index, serve, list, seed). Write safety: file-based locking (fcntl on Unix) for concurrent access. Observability: OpenTelemetry (opt-in), Prometheus /metrics endpoint, trace events embedded in every query response. Testing: 160+ automated tests covering all core features.</action>
  <verify>All specs match pyproject.toml and implementation. Test count is 160+ as stated in PROJECT.md.</verify>
</task>

<task type="auto" mode="co-author">
  <name>DuckDB 8-table schema</name>
  <action>Write ~150 words describing the DuckDB schema with all 8 tables. Present as a table or structured list: (1) documents: URI, namespace, content hash, checksums. (2) chunks: text chunks with document foreign keys, position. (3) embeddings: 384-dim float vectors per chunk. (4) metadata: key-value pairs per document for filtering. (5) file_index: incremental indexing state (path, SHA256, mtime). (6) lexical_postings: BM25 token-to-chunk mapping with term frequency. (7) scientific_measurements: raw_value, raw_unit, canonical_value, canonical_unit per chunk -- the core table enabling dimensional conversion. (8) scientific_formulas: raw_formula, normalized_formula per chunk. Explain why DuckDB: embedded (no external service), SQL-native (BM25 implemented as SQL queries), single-file (easy deployment on HPC nodes).</action>
  <verify>8 tables match storage/duckdb_store.py. Table names and columns are accurate. DuckDB rationale is sound for HPC context.</verify>
</task>

<task type="auto" mode="co-author">
  <name>HDF5 connector details</name>
  <action>Write ~100 words on the HDF5 connector implementation. Uses h5py library. Walks HDF5 group hierarchy and datasets. Extracts: dataset names, shapes, dtypes, all attributes (including units, descriptions, long_name). Measurement extraction from attribute values: parses attributes like "pressure_unit: kPa", "pressure_value: 250" and feeds them through the same SI canonicalization pipeline as text connectors. Chunks attribute metadata as searchable text for hybrid retrieval. Example: an HDF5 file with dataset "/experiment/pressure" having attribute units="kPa" becomes searchable via dimensional conversion operators.</action>
  <verify>Matches connectors/ HDF5 implementation. h5py is in dependencies.</verify>
</task>

<task type="auto" mode="co-author">
  <name>NetCDF connector details</name>
  <action>Write ~100 words on the NetCDF connector implementation. Uses xarray library for CF convention awareness. Opens NetCDF datasets and extracts: variable names, units, long_name, dimensions from each variable; global attributes (title, history, conventions). Measurement extraction from variable metadata follows the same SI canonicalization pipeline. CF convention support: recognizes standard_name, units, and cell_methods attributes. Example: a climate NetCDF file with variable "temperature" having units="K" and "pressure" having units="hPa" becomes searchable with dimensional operators across both variables.</action>
  <verify>Matches connectors/ NetCDF implementation. xarray is in dependencies. CF convention handling is accurate.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Experimental platform</name>
  <action>Write ~150 words describing the experimental platform. Hardware: [specify machine -- to be filled with actual specs]. OS: Linux. Software versions: Python 3.11, DuckDB [version], sentence-transformers [version], h5py [version], xarray [version]. Embedding model: all-MiniLM-L6-v2 (22.7M parameters, 384-dim, Apache 2.0 license). Codebase: approximately 10,000 lines of Python, 160+ automated tests, single-command setup via "uv sync". All code and benchmarks will be open-source. Reproducibility: fixed random seeds, deterministic DuckDB operations, version-pinned dependencies via pyproject.toml. Note: Qdrant and Neo4j connectors use in-memory implementations for evaluation -- real distributed deployment is future work.</action>
  <verify>Codebase size ~10,000 lines matches PROJECT.md. Honest about in-memory stubs for Qdrant/Neo4j. Platform details are reproducible.</verify>
</task>
