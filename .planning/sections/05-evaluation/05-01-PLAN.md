---
section: 5
plan: 1
wave: 3
word_budget: 950
---

# Plan 05-01: Experimental Setup + RQ1 Dimensional Conversion + RQ2 Formula Matching

## Context
This plan covers the evaluation methodology, benchmark construction, and the first two research questions: dimensional conversion results (RQ1, the core contribution) and formula matching results (RQ2). Since benchmarks have not been run yet, specific result numbers use [TBD] placeholders, but the full evaluation framework, methodology, and expected analysis structure are specified.

## Tasks

<task type="auto" mode="co-author">
  <name>Benchmark construction</name>
  <action>Write ~150 words describing the benchmark construction methodology. Scientific retrieval tasks with deliberate unit-variation across documents. 5 SI domains: distance (mm/cm/m/km), mass (mg/g/kg), time (s/min/h), pressure (Pa/kPa/MPa), velocity (m/s, km/h). Each task structure: query expressed in one unit, relevant documents containing the same physical quantity in 2-3 different unit representations. [TBD] total tasks across all domains. Control condition: same-unit queries (where baselines should perform equally) to isolate the dimensional conversion effect. Cross-unit queries: the target condition where our system should show advantage. Ground truth: manually annotated relevance judgments for each query-document pair.</action>
  <verify>5 domains match the 13-unit implementation. Task structure is sound for isolating dimensional conversion effect. [TBD] used for numbers not yet determined.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Baselines description</name>
  <action>Write ~100 words describing the 5 baselines. (1) BM25-only: lexical retrieval with Okapi BM25 (k1=1.2, b=0.75). (2) Dense vector-only: all-MiniLM-L6-v2 embeddings with cosine similarity. (3) Hybrid BM25+vector: standard fusion without science operators. (4) String normalization: Numbers Matter!-style approach that normalizes unit strings but does not perform arithmetic conversion. (5) Our full pipeline: hybrid 4-branch retrieval with science-aware operators (dimensional conversion + formula normalization). Baselines 1-3 represent standard retrieval; baseline 4 represents the best prior quantity-aware approach.</action>
  <verify>Baselines are fair -- standard retrieval + best prior approach. Our system includes all components.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Metrics</name>
  <action>Write ~50 words listing evaluation metrics. Precision@K (K=1,3,5,10), Recall@K, F1@K, and MRR (Mean Reciprocal Rank). Report at K=5 as the primary comparison point. Statistical significance via paired t-test or bootstrap confidence intervals across queries.</action>
  <verify>Standard IR metrics. K=5 is reasonable for scientific retrieval tasks.</verify>
</task>

<task type="auto" mode="co-author">
  <name>RQ1: Dimensional conversion results</name>
  <action>Write ~350 words for RQ1: "Does dimensional conversion improve retrieval over baselines on cross-unit queries?" Structure the analysis as follows:

Overall results table (Table 3): [TBD] -- Precision@5, Recall@5, F1@5, MRR for all 5 baselines on cross-unit queries.

Same-unit control: Report [TBD] results showing baselines perform comparably on same-unit queries, confirming that our advantage is specific to cross-unit scenarios.

Cross-unit results: Report [TBD] results. Expected pattern: on cross-unit queries, baselines 1-3 find only documents matching the query unit representation; baseline 4 (string normalization) may find some lexical variants but cannot cross SI prefixes; our system finds ALL unit variants because canonical comparison is exact.

Per-domain breakdown: Report [TBD] results across the 5 SI domains. Discuss whether some domains show larger improvements (e.g., pressure with Pa/kPa/MPa may show more variation than mass with g/kg).

Reference Figure 4: bar chart showing Recall@5 across baselines on cross-unit queries.

Key finding: [TBD] -- expected to show that dimensional conversion recovers false negatives that all baselines miss, with the improvement concentrated entirely on cross-unit queries.</action>
  <verify>All specific numbers marked [TBD]. Analysis structure is complete and sound. Figure 4 referenced. Statistical testing mentioned.</verify>
</task>

<task type="auto" mode="co-author">
  <name>RQ2: Formula matching results</name>
  <action>Write ~250 words for RQ2: "Does formula normalization improve retrieval on formula-variant queries?" Structure:

Formula-variant task set: queries containing a formula in one notation with relevant documents containing the formula in alternative notations (whitespace variants, superscript variants, side-swapped, factor-reordered).

Results: [TBD] -- Precision@5, Recall@5 with and without formula operators active. Expected: without normalization, only exact string matches are found; with normalization, all notation variants are matched.

Integration effect: [TBD] -- results for formula + dimensional combined vs each alone. Test whether the two operator types provide independent value or whether their benefits overlap. Expected: largely independent because they address orthogonal aspects of scientific content (quantities vs equations).

Key finding: [TBD] -- formula normalization adds retrieval coverage for equation-bearing scientific documents, and the combination with dimensional operators is additive.</action>
  <verify>All specific numbers marked [TBD]. Integration analysis tests the novel combination claim. Analysis is methodologically sound.</verify>
</task>
