---
section: 5
plan: 2
wave: 3
word_budget: 925
---

# Plan 05-02: RQ3 Federated Search + Ablation + Agentic Results + Indexing Performance

## Context
This plan covers the remaining evaluation: federated search results (RQ3), the ablation study showing incremental contribution of each retrieval branch, multi-hop agentic retrieval results, and indexing performance benchmarks. All specific result numbers use [TBD] placeholders since benchmarks have not been run yet.

## Tasks

<task type="auto" mode="co-author">
  <name>RQ3: Federated search results</name>
  <action>Write ~250 words for RQ3: "Does federated search discover results hidden in other backends?" Structure:

Experimental setup: same scientific corpus distributed across 3 backends -- filesystem, S3, and HDF5. Documents with different unit representations placed in different backends to simulate realistic HPC data distribution.

Single-backend vs multi-namespace: [TBD] -- coverage (% of relevant documents found) for filesystem-only, S3-only, HDF5-only, and federated (all three). Expected: single-backend search misses documents stored in other tiers; federated search finds all relevant documents regardless of storage location.

Science-aware federation advantage: [TBD] -- compare federated search WITH dimensional operators vs federated search WITHOUT. Even when federating, standard retrieval misses cross-unit matches. Science-aware operators provide value on top of federation.

Key finding: [TBD] -- federation alone improves coverage, but federation combined with dimensional conversion provides the full benefit, recovering documents that are both in different backends AND in different unit representations.</action>
  <verify>All numbers [TBD]. Experimental setup is realistic for HPC. Analysis isolates federation effect from science-operator effect.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Ablation study</name>
  <action>Write ~275 words for the ablation study showing incremental contribution of each retrieval branch. Configurations tested:

(1) Lexical only (BM25)
(2) Vector only (dense embedding)
(3) Lexical + Vector (standard hybrid)
(4) Lexical + Vector + Scientific operators
(5) Lexical + Vector + Scientific + Graph
(6) Full pipeline (all branches + agentic)

Results table: [TBD] -- F1@5 for each configuration on the full benchmark.

Analysis: [TBD] -- expected pattern: each component adds independent value. Lexical provides high precision on keyword matches. Vector adds semantic similarity. Scientific operators provide the largest marginal gain on cross-unit and formula-variant queries (the novel contribution). Graph adds context expansion. Agentic multi-hop adds iterative refinement.

Reference Figure 5: ablation chart showing incremental F1@5 as branches are added.

Key finding: [TBD] -- scientific operators are expected to contribute the largest marginal improvement on science-specific queries, validating the core thesis that science-aware retrieval branches provide value beyond standard hybrid retrieval.</action>
  <verify>All numbers [TBD]. 6 configurations provide clean incremental analysis. Figure 5 referenced.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Multi-hop agentic retrieval results</name>
  <action>Write ~200 words on multi-hop agentic retrieval evaluation. Compare single-pass retrieval vs multi-hop (2 hops, 3 hops) on complex queries requiring multiple pieces of evidence or queries where initial results are insufficient.

Results: [TBD] -- Recall@5 for single-pass vs 2-hop vs 3-hop. Expected: multi-hop improves recall on complex queries by iteratively refining the search based on initial results.

Convergence analysis: [TBD] -- average number of hops before convergence across query types. Report distribution of queries converging at each hop count.

Query rewriting examples: provide 2-3 concrete examples showing how the LLM rewrites queries across hops: initial query -> expanded/narrowed/pivoted query -> final query. Show how each rewrite leads to better results. Include at least one example using the fallback (non-LLM) rewriting for reproducibility.

Key finding: [TBD] -- multi-hop retrieval with LLM rewriting improves coverage on complex queries, with most queries converging within 2-3 hops.</action>
  <verify>All numbers [TBD]. Concrete examples will be written after benchmarks run. Fallback mode included for reproducibility.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Indexing performance</name>
  <action>Write ~150 words on indexing performance benchmarks.

Full index time: [TBD] -- time to index the complete benchmark corpus, reporting documents/second throughput.

Incremental index: [TBD] -- time to re-index with 5%, 10%, and 25% corpus changes. Expected: incremental indexing is significantly faster than full re-indexing due to SHA256 change detection skipping unchanged files.

Per-connector overhead: [TBD] -- indexing time breakdown by connector type (filesystem vs S3 vs HDF5 vs NetCDF). HDF5/NetCDF may have higher per-document overhead due to hierarchical metadata extraction.

Reference Figure 6: chart comparing incremental vs full indexing time as a function of corpus change percentage.

Key finding: [TBD] -- incremental indexing enables efficient maintenance of evolving scientific corpora, with overhead proportional to the fraction of changed documents rather than total corpus size.</action>
  <verify>All numbers [TBD]. Change percentages (5/10/25%) are reasonable test points. Figure 6 referenced.</verify>
</task>
