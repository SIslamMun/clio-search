---
section: 3
plan: 1
wave: 2
word_budget: 650
---

# Plan 03-01: Architecture Overview + Dimensional Conversion Operators

## Context
This plan covers the system architecture overview and the dimensional conversion operators -- the core novelty of the paper. The architecture overview provides a high-level block diagram of the full pipeline. The dimensional conversion subsection gets the most space in the design section because it is the primary technical contribution: deterministic arithmetic SI conversion at index and query time, guaranteed correct by construction.

## Tasks

<task type="auto" mode="co-author">
  <name>Architecture overview and block diagram</name>
  <action>Write ~250 words describing the system architecture with reference to Figure 1 (block diagram). Pipeline: Query Interface (FastAPI + CLI) -> Namespace Registry -> Retrieval Coordinator -> Parallel Branches (lexical, vector, graph, scientific) -> Merge and Rerank -> Citations with traces. 6 connector types: filesystem, S3, Qdrant, Neo4j, HDF5, NetCDF/xarray. Describe capability negotiation: the coordinator discovers what each backend supports at registration time, routes queries only to capable backends. Mention trace events: every query produces stage-level traces for observability. Emphasize the pluggable design: new operators and connectors can be added without modifying the core pipeline.</action>
  <verify>Architecture matches implementation: retrieval/coordinator.py, connectors/ directory. Figure 1 referenced but not created (figure creation is separate). All 6 connector types listed.</verify>
</task>

<task type="auto" mode="co-author">
  <name>SI conversion table (Table 2)</name>
  <action>Write ~75 words introducing Table 2: the SI conversion table covering 5 domains. Domains and mappings: Distance (mm, cm, m, km -> m), Mass (mg, g, kg -> kg), Time (s, min, h -> s), Pressure (Pa, kPa, MPa -> Pa), Velocity (m/s, km/h -> m/s). Each entry has a conversion factor (e.g., kPa -> Pa: multiply by 1000). Explain that the table is extensible and deterministic -- adding a unit requires only its conversion factor to the base SI unit.</action>
  <verify>13 units across 5 domains matches indexing/scientific.py lines 79-85. Conversion factors are physically correct.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Measurement extraction pipeline</name>
  <action>Write ~100 words describing measurement extraction at index time. Regex pattern extracts numeric values with unit suffixes from text. For each match, the system stores a 4-tuple: (raw_value, raw_unit, canonical_value, canonical_unit) in the DuckDB scientific_measurements table. Example: "200 kPa" -> (200, "kPa", 200000, "Pa"). Canonicalization happens once at index time, so query-time comparison is a simple numeric operation against pre-computed canonical values. Reference Figure 2 (dimensional conversion pipeline diagram).</action>
  <verify>Matches storage/duckdb_store.py scientific_measurements table schema. Index-time canonicalization is accurate.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Query-time operators</name>
  <action>Write ~125 words describing the three query-time dimensional operators. (1) Numeric range: --numeric-range "190:360:kPa" canonicalizes both bounds to base SI (190000:360000 Pa), then performs range comparison against canonical values in the index. (2) Unit match: --unit-match "5:kg" finds documents with measurements matching a specific value with configurable tolerance. (3) Value match: exact canonical comparison. Walk through the motivating example from Section 1: 3 documents storing "200 kPa", "200000 Pa", and "0.2 MPa" across filesystem, S3, and HDF5 -- all three are found because all canonicalize to 200000 Pa.</action>
  <verify>CLI flags match implementation. Motivating example is consistent with Section 1.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Correctness guarantee argument</name>
  <action>Write ~100 words contrasting the correctness properties of three approaches. (1) String normalization (Numbers Matter!): "kilopascal" and "pascal" remain different strings -- no match. (2) Learned embeddings (CONE): depends on training data, 0.54 accuracy on numeric content (Numeracy Gap). (3) Our arithmetic approach: 200 x 1000 = 200000, deterministic and guaranteed correct by construction. If the SI conversion factor is correct (and these are physical constants), the match is correct. This is a stronger guarantee than any learning-based approach can provide. Cite Theoretical Limits of Embedding Retrieval (arXiv:2508.21038) for formal bounds on embedding limitations.</action>
  <verify>Argument is logically sound. Does not overclaim -- we acknowledge our coverage is limited to 13 units (vs CQE's 531). The guarantee applies to supported units only.</verify>
</task>
