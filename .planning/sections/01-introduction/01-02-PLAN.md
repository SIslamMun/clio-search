---
section: 1
plan: 2
wave: 1
word_budget: 425
---

# Plan 01-02: Contributions List + Paper Organization

## Context
This plan covers the numbered contributions list and the paper organization paragraph. The contributions must be precise, differentiated from prior work, and map directly to evaluation sections. The organization paragraph provides a roadmap for the reader.

## Tasks

<task type="auto" mode="co-author">
  <name>Contributions preamble</name>
  <action>Write a brief 1-2 sentence lead-in stating that this paper introduces science-aware retrieval operators as a new class of retrieval primitives. Frame them as pluggable, deterministic (for dimensional), and composable with standard hybrid retrieval.</action>
  <verify>Framing matches the narrowed claims in GAP-VALIDATION.md. Does not overclaim "agentic" beyond what is implemented.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Contribution 1: Dimensional conversion measurement retrieval</name>
  <action>Write contribution item: Dimensional-conversion measurement retrieval that canonicalizes quantities to base SI units via multiplication (e.g., kPa x 10^3 = Pa). Guaranteed correct by construction. Supports numeric range queries, unit matching, and tolerance-based comparison across 13 units in 5 SI domains.</action>
  <verify>Matches implementation: indexing/scientific.py, 13 units, 5 domains. "Guaranteed correct" claim is justified for deterministic arithmetic.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Contribution 2: Unified formula normalization</name>
  <action>Write contribution item: Unified formula normalization that canonicalizes formulas across whitespace, superscript notations, side-swap, and factor reordering. Integrated with dimensional operators as parallel branches in the hybrid retrieval pipeline. No prior system combines formula matching with quantity-aware retrieval.</action>
  <verify>Matches implementation: indexing/scientific.py lines 88-114. Gap claim validated in GAP-VALIDATION.md.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Contribution 3: Federated multi-namespace search</name>
  <action>Write contribution item: Federated multi-namespace search with a connector architecture supporting filesystem, S3, Qdrant, Neo4j, HDF5, and NetCDF backends. Per-backend capability negotiation ensures each namespace contributes only the retrieval modalities it supports. Incremental indexing with SHA256 change detection enables efficient re-indexing of evolving corpora.</action>
  <verify>All 6 connector types implemented. Capability negotiation is real. Incremental indexing is real.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Contribution 4: HDF5/NetCDF metadata indexing</name>
  <action>Write contribution item: HDF5 and NetCDF metadata indexing connectors that extract datasets, attributes, variables, dimensions, and units from scientific file formats. Measurements extracted from these formats pass through the same SI canonicalization pipeline as text-based connectors, enabling cross-format dimensional search.</action>
  <verify>HDF5 connector (h5py) and NetCDF connector (xarray) are implemented and tested.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Contribution 5: Multi-hop agentic retrieval</name>
  <action>Write contribution item: Multi-hop agentic retrieval with LLM-driven query rewriting (expand, narrow, pivot strategies) and iterative search refinement over federated scientific data. Each hop executes the full hybrid pipeline with a refined query, converging when results stabilize or a maximum hop count is reached.</action>
  <verify>Multi-hop loop and LLM rewriting are implemented. Strategies match code: expand/narrow/pivot/done.</verify>
</task>

<task type="auto" mode="co-author">
  <name>Paper organization paragraph</name>
  <action>Write a brief paragraph (~125 words) outlining the paper structure: Section 2 covers background and related work across hybrid retrieval, numeracy, quantity-aware retrieval, and scientific data discovery. Section 3 presents the system design including dimensional operators, formula normalization, hybrid pipeline, federated search, and agentic loop. Section 4 details the implementation. Section 5 evaluates across dimensional conversion, formula matching, federated search, and ablation. Section 6 concludes with limitations and future work.</action>
  <verify>Section numbers match outline.md. All major topics mentioned.</verify>
</task>
